services:

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: gestion-frontend
    ports:
      - "4200:4200"
    volumes:
      - ./src:/app/src
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
